#! /usr/bin/env node

"use strict";

var path = require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(_filename)), './lib');

require(lib + '/etest.js').convert();
